# registry
build_registry_name: "{{ lookup('env','BUILD_REGISTRY_NAME') | default('docker-registry.jx.krestomat.io',true) }}"
build_project_registry_path: "{{ lookup('env','BUILD_PROJECT_REGISTRY_PATH') | default(ctr_repo_owner+'/'+ctr_repo_subdir,true) }}"
