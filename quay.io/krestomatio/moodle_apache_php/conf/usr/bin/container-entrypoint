#!/bin/bash -e
# description: entrypoint script for container

# first arg is `-f` or `--some-option
if [ "${1#-}" != "$1" ]; then
    if ! [ -z ${CMD+x} ]; then
	       set -- ${CMD} "$@"
    fi
fi

exec "$@"
