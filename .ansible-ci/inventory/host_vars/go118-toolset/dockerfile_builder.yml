# platform vars
go_version: "{{ go_versions.118 }}"
go_version_major_minor: "{{ go_version.split('.')[0] + '.' + go_version.split('.')[1] }}"

version_image_suffix: toolset
template_path: "{{ template_dir }}/go-toolset"

# builder
dockerfile_builder_image_ocp_tags: "go,go-toolset,go-toolset{{ go_version_major_minor }}"
