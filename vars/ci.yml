# build playbook
play_containers: "{{ lookup('env','PLAY_CONTAINERS') | default('all:!ci',true) }}"
play_diff: "{{ lookup('env','PLAY_DIFF') | default(true,true) }}"

# provider
cloud_provider: "{{ lookup('env','CLOUD_PROVIDER') | default('none',true) }}"
cloud_providers:
  - gke
  - m6e
